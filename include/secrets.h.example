#pragma once

// Copy this file to secrets.h (same directory) and fill in your network details.
// secrets.h is ignored by git so your credentials stay local.

#define WIFI_SSID_VALUE "your-ssid-here"
#define WIFI_PASSWORD_VALUE "your-password-here"
